/**
 * @jest-environment node
 */

import { describe, expect, it } from "@jest/globals";

describe("Replacement effects", () => {
  it.skip("Does not reduce cost for Chars when the effect is for action", () => {
    expect(true).toBeTruthy();
  });
});
