/**
 * @jest-environment node
 */

import { describe, expect, it } from "@jest/globals";
import { TestStore } from "@lorcanito/engine/rules/testStore";
import { luciferCunningCat } from "@lorcanito/engine/cards/ROF/characters/characters";

describe("Lucifer - Cunning Cat", () => {
  it.todo(
    "**MOUSE CATCHER** When you play this character, each opponent chooses and discards either 2 cards or 1 action card.",
  );
});
